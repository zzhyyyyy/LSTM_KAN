"""单输出·多步基础模型包：每个目标一个模型，一次输出该目标的 t+1/t+2/t+3。

与 multi_output_model 共享设定（原始尺度、多步、分步优化、不含 Algae_Sum 输入、7:1:2），
唯一区别是每个目标各训练一个模型（4 架构 × 5 目标 = 20 个），目标精度通常优于联合多输出。
旧 single_output_model（log 尺度）保留作历史对比。
"""
